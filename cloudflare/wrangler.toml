name = "polymarket-copy-signal"
main = "worker.js"
compatibility_date = "2024-11-01"

# Fire the poller every 5 minutes (standard 5-field cron, UTC). Safe now that
# data is served from Supabase Storage (no per-cycle git commit / Pages build).
[triggers]
crons = ["*/5 * * * *"]
