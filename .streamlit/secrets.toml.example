# Copy this to .streamlit/secrets.toml for LOCAL dashboard runs, or paste the
# same two lines into Streamlit Community Cloud -> Advanced settings -> Secrets.
# NEVER commit the real secrets.toml (it is gitignored).
SUPABASE_URL = "https://YOUR-PROJECT.supabase.co"
SUPABASE_KEY = "your-service-role-key"
